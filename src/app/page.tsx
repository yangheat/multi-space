import { ModeToggle } from '../components/ui/ModeToggle' // Adjusted the import path

export default function Home() {
  return (
    <div>
      <ModeToggle />
    </div>
  ) // Simplified the JSX syntax
}
